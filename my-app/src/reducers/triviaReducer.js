

const initialState = {
    jokeData: [],
    isFetchingData: false,
    error: '',
    displayAnswer: false
};

export const triviaReducer = (state=initialState, action) => {
    switch (action.type) {


        default:
            return state;
    }
};