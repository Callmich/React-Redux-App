

const initialState = {
    jokeData: [],
    isFetchingData: false,
    error: ''
};

export const jokerReducer = (state=initialState, action) => {
    switch (action.type) {


        default:
            return state;
    }
};